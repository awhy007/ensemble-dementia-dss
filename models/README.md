# Model bundle

`selected_model_bundle.joblib` is the locked soft-voting model produced in Python 3.13.15 with scikit-learn 1.6.1, NumPy 2.1.3 and pandas 2.2.3.

Only load model files obtained from this trusted project repository. Joblib/pickle files can execute code when loaded and should not be accepted from untrusted sources.

